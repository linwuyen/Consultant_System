const state={rows:[],staticRows:[],total:0,page:1,pageSize:50,view:'table',stats:null,mode:'api'};
const $=id=>document.getElementById(id);
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
const fmtDate=v=>{if(!v)return'—';const d=new Date(`${v}T00:00:00Z`);return Number.isNaN(d.getTime())?v:d.toLocaleDateString('zh-TW',{year:'numeric',month:'2-digit',day:'2-digit'});};
const pct=(n,d)=>d?`${Math.round(n/d*100)}%`:'0%';
const ageDays=v=>{if(!v)return Infinity;return Math.max(0,Math.floor((Date.now()-new Date(`${v}T00:00:00Z`).getTime())/86400000));};
const fmtUpdated=v=>v?new Date(v).toLocaleString('zh-TW',{timeZone:'Asia/Taipei',hour12:false}):'—';
async function getJSON(url){const r=await fetch(url,{cache:'no-store'});if(!r.ok)throw new Error(`${r.status} ${r.statusText}`);return r.json();}
function addOptions(id,items){for(const x of items)$(id).insertAdjacentHTML('beforeend',`<option value="${esc(x)}">${esc(x)}</option>`);}
function staticStats(rows,updatedAt){const companies=['McKinsey','BCG','Deloitte','PwC'].map(company=>{const r=rows.filter(x=>x.company===company),d=r.filter(x=>x.date).length,latest=r.map(x=>x.date).filter(Boolean).sort().reverse()[0]||null;return{company,records:r.length,dated:d,latest}});return{updated_at:updatedAt,total:rows.length,dated:rows.filter(x=>x.date).length,latest:rows.map(x=>x.date).filter(Boolean).sort().reverse()[0]||null,companies};}
async function setupFilters(){if(state.mode==='api'){const f=await getJSON('./api/filters');addOptions('company',f.companies||[]);addOptions('topic',f.topics||[]);addOptions('year',f.years||[]);}else{addOptions('company',[...new Set(state.staticRows.map(r=>r.company).filter(Boolean))].sort());addOptions('topic',[...new Set(state.staticRows.flatMap(r=>r.topics||[]))].sort());addOptions('year',[...new Set(state.staticRows.map(r=>(r.date||'').slice(0,4)).filter(Boolean))].sort().reverse());}}
function renderDashboard(){const s=state.stats||{},total=Number(s.total||0),dated=Number(s.dated||0),latest=s.latest||'',topicCount=$('topic').options.length-1;$('kpis').innerHTML=`<div class="kpi"><div class="kpi-label">Total records</div><div class="kpi-value">${total.toLocaleString()}</div><div class="kpi-sub">${state.mode==='api'?'Cloudflare D1':'Static fallback'} 研究紀錄</div></div><div class="kpi"><div class="kpi-label">Dated records</div><div class="kpi-value">${pct(dated,total)}</div><div class="kpi-sub">${dated.toLocaleString()} 筆有發布日期</div></div><div class="kpi"><div class="kpi-label">Latest publication</div><div class="kpi-value">${fmtDate(latest)}</div><div class="kpi-sub">${Number.isFinite(ageDays(latest))?`${ageDays(latest)} 天前`:'尚無日期'}</div></div><div class="kpi"><div class="kpi-label">Topic tags</div><div class="kpi-value">${topicCount}</div><div class="kpi-sub">可查詢維度</div></div>`;$('updatedAt').textContent=`資料更新 ${fmtUpdated(s.updated_at)}`;const hours=s.updated_at?(Date.now()-new Date(s.updated_at).getTime())/3600000:Infinity;$('freshness').className=`freshness ${hours<=36?'good':hours<=72?'':'bad'}`;$('freshness').innerHTML=`<span class="dot"></span><span>${state.mode==='api'?'D1 / Worker':'GitHub JSON fallback'} · ${hours<=36?'資料新鮮':hours<=72?'資料需留意':'資料可能過期'}</span>`;const map=Object.fromEntries((s.companies||[]).map(x=>[x.company,x]));$('coverage').innerHTML=['McKinsey','BCG','Deloitte','PwC'].map(name=>{const c=map[name]||{records:0,dated:0,latest:null},n=Number(c.records||0),d=Number(c.dated||0),age=ageDays(c.latest);let level='bad',label='FAIL';if(n>=3&&d===n&&age<=60){level='good';label='PASS'}else if(n>0&&d>0){level='warn';label='PARTIAL'}return `<div class="coverage-card"><div class="coverage-top"><span class="coverage-name">${name}</span><span class="badge ${level}">${label}</span></div><div class="coverage-numbers"><div class="metric-mini"><strong>${n}</strong><span>Records</span></div><div class="metric-mini"><strong>${pct(d,n)}</strong><span>Dated</span></div><div class="metric-mini"><strong>${fmtDate(c.latest)}</strong><span>Latest</span></div><div class="metric-mini"><strong>${n-d}</strong><span>Undated</span></div></div></div>`}).join('');}
function queryParams(){const p=new URLSearchParams({page:String(state.page),page_size:String(state.pageSize),sort:$('sort').value});for(const [id,key] of [['q','q'],['company','company'],['topic','topic'],['year','year']]){const v=$(id).value.trim();if(v)p.set(key,v)}return p;}
function staticQuery(){const q=$('q').value.trim().toLowerCase(),company=$('company').value,topic=$('topic').value,year=$('year').value,sort=$('sort').value;let rows=state.staticRows.filter(r=>{const hay=`${r.company||''} ${r.title||''} ${r.description||''} ${(r.topics||[]).join(' ')}`.toLowerCase();return(!q||hay.includes(q))&&(!company||r.company===company)&&(!topic||(r.topics||[]).includes(topic))&&(!year||(r.date||'').startsWith(year))});rows.sort((a,b)=>sort==='date-asc'?(a.date||'9999').localeCompare(b.date||'9999'):sort==='company'?(a.company||'').localeCompare(b.company||'')||(b.date||'').localeCompare(a.date||''):sort==='title'?(a.title||'').localeCompare(b.title||''):(b.date||'').localeCompare(a.date||''));state.total=rows.length;state.rows=rows.slice((state.page-1)*state.pageSize,state.page*state.pageSize);}
async function loadRecords(reset=true){if(reset)state.page=1;state.pageSize=Number($('pageSize').value)||50;$('resultCount').textContent='查詢中…';if(state.mode==='api'){const data=await getJSON(`./api/reports?${queryParams()}`);state.rows=data.reports||[];state.total=Number(data.total||0);state.page=Number(data.page||state.page);}else staticQuery();renderRecords();}
function renderTable(rows){$('tableBody').innerHTML=rows.map(r=>`<tr><td class="date-cell">${fmtDate(r.date)}</td><td class="company-cell">${esc(r.company)}</td><td><a class="title-link" href="${esc(r.url)}" target="_blank" rel="noopener noreferrer">${esc(r.title)} ↗</a>${r.description?`<div class="row-desc">${esc(r.description)}</div>`:''}</td><td><div class="tags">${(r.topics||[]).map(t=>`<span class="tag">${esc(t)}</span>`).join('')||'<span class="tag">未分類</span>'}</div></td><td><a class="source-link" href="${esc(r.url)}" target="_blank" rel="noopener noreferrer">${esc(r.source_name||'官方來源')}</a></td></tr>`).join('');}
function renderCards(rows){$('cardView').innerHTML=rows.map(r=>`<article class="research-card"><div class="card-meta"><strong>${esc(r.company)}</strong><span>·</span><span>${fmtDate(r.date)}</span></div><h3><a href="${esc(r.url)}" target="_blank" rel="noopener noreferrer">${esc(r.title)} ↗</a></h3>${r.description?`<p>${esc(r.description)}</p>`:''}<div class="tags">${(r.topics||[]).map(t=>`<span class="tag">${esc(t)}</span>`).join('')}</div></article>`).join('');}
function renderRecords(){const pages=Math.max(1,Math.ceil(state.total/state.pageSize));renderTable(state.rows);renderCards(state.rows);$('resultCount').textContent=`${state.total.toLocaleString()} 筆符合條件`;$('activeFilters').textContent=state.mode==='api'?' · Server-side SQL':' · Static fallback';$('qualityInline').innerHTML=`<span class="quality-chip">${state.mode==='api'?'Cloudflare D1':'GitHub JSON'}</span>`;$('empty').hidden=state.total!==0;$('tableView').hidden=state.view!=='table'||state.total===0;$('cardView').hidden=state.view!=='card'||state.total===0;const start=state.total?(state.page-1)*state.pageSize+1:0,end=Math.min(state.page*state.pageSize,state.total);$('pageInfo').textContent=`${start.toLocaleString()}–${end.toLocaleString()} / ${state.total.toLocaleString()} · 第 ${state.page}/${pages} 頁`;$('prevPage').disabled=state.page<=1;$('nextPage').disabled=state.page>=pages;}
function setView(v){state.view=v;$('tableViewBtn').classList.toggle('active',v==='table');$('cardViewBtn').classList.toggle('active',v==='card');renderRecords();}
function clearFilters(){$('q').value='';$('company').value='';$('topic').value='';$('year').value='';$('sort').value='date-desc';$('pageSize').value='50';loadRecords(true);}
async function init(){try{await getJSON('./api/health');state.mode='api';await setupFilters();state.stats=await getJSON('./api/stats');}catch{state.mode='static';const payload=await getJSON('./data/reports.json');state.staticRows=payload.reports||[];await setupFilters();state.stats=staticStats(state.staticRows,payload.updated_at);}renderDashboard();await loadRecords(true);for(const id of ['company','topic','year','sort','pageSize'])$(id).addEventListener('change',()=>loadRecords(true));let timer;$('q').addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(()=>loadRecords(true),250)});$('clearFilters').addEventListener('click',clearFilters);$('prevPage').addEventListener('click',()=>{if(state.page>1){state.page--;loadRecords(false)}});$('nextPage').addEventListener('click',()=>{const pages=Math.ceil(state.total/state.pageSize);if(state.page<pages){state.page++;loadRecords(false)}});$('tableViewBtn').addEventListener('click',()=>setView('table'));$('cardViewBtn').addEventListener('click',()=>setView('card'));}
init().catch(e=>{$('freshness').className='freshness bad';$('freshness').innerHTML='<span class="dot"></span><span>資料載入失敗</span>';$('resultCount').textContent=`錯誤：${e.message}`;});
